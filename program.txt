IMPORT stdlib

MODULE SERVER
	FUNCTION MAIN
		VAR a AS INTEGER
		VAR b AS INTEGER
		
		a = 12
		b = 3

		println("" + (a + b))
		println("" + (a - b))
		println("" + (a * b))
		println("" + (a / b))
		println("" + (a < b))
		println("" + (a = a))
		println("" + (a = b))
		
	END FUNCTION
END MODULE
